package create

import (
	"github.com/spf13/cobra"
{{range .Resources}}
	"{{$.Module}}/cmd/{{$.Binary}}/create/{{.NameLower}}"
{{- end}}
)

var Cmd = &cobra.Command{
	Use:   "create RESOURCE",
	Short: "Create a resource",
	Long:  "Create a resource",
}

func init() {
{{- range .Resources}}
	Cmd.AddCommand({{.NameLower}}.Cmd)
{{- end}}
}
